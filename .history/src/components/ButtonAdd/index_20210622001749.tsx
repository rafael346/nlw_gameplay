import { RectButton, RectButtonProps } from 'react-native-gesture-handler'
import { View, Text } from 'react-native'
import { styles } from './styles'

export default function ButtonAdd({ }: RectButtonProps) {
  return (
    <RectButton
      style={styles.container}>

    </RectButton>
  )
}
